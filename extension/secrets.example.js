// Copy this file to secrets.js and fill in your real keys.
// secrets.js is gitignored — it is never committed, so real keys never hit GitHub.
const MV_ANTHROPIC_KEY = '';
const MV_DEEPGRAM_KEY = '';
